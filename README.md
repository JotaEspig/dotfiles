# JotaEspig's dotfiles

## Installation:

```bash
git clone https://github.com/JotaEspig/dotfiles
cd dotfiles
```

Install .zshrc:
```
cp .zshrc ~/.zshrc
```

The others dotfiles you can do it like this:
```bash
cp nvim ~/.config
cp hypr ~/.config
...
```
